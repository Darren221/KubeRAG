# Introduction to Kubernetes

Kubernetes is an open-source container orchestration system.
It manages containerized applications across clusters of machines.

## Core Concepts

### Pods

A pod is the smallest deployable unit in Kubernetes.
Pods host one or more containers that share storage and network.

### Services

A service exposes a set of pods at a stable network endpoint.
Different service types support different access patterns.
